import React, { Component } from 'react'
import { Text, StyleSheet, View, ScrollView } from 'react-native'

export default class ListMenuAndItem extends Component {
    render() {
        return (
            <ScrollView>
                
            </ScrollView>
        )
    }
}

const styles = StyleSheet.create({})
